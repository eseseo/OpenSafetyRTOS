# ---------------------------------------------------------------------------
# tests/integration/CMakeLists.txt
# Integration tests for OpenSafetyRTOS.
#
# Phase 0 stub: no test sources yet. Integration tests may require:
#   - QEMU emulation target
#   - Hardware-in-the-loop (HIL) runner
#   - A separate toolchain for the target
# ---------------------------------------------------------------------------

message(STATUS "Integration tests: no sources yet (Phase 0 stub)")
